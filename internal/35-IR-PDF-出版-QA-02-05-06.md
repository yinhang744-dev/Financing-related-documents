# IR PDF 出版 QA — 02 / 05 / 06（逐页人工）

| **文档控制（IR）** | |
|------|------|
| **Owner** | IR / PM |
| **Version** | 1.0.0-ir |
| **Status** | active |
| **Classification** | internal |
| **Last Updated** | 2026-05-16 |
| **SSOT** | 导出流水线 `scripts/tools/investor_ir_pandoc_pdf.py` · [33 — Data Room 与终审](33-投资人Data-Room导出包与IR法务终审清单.md) |

## 适用范围与阶次

叙事类 PDF 由 `build-investor-ir-pdf-pack.py` 经 **Pandoc → DOCX（`reference-ir.docx`）→ `ir-native-toc.lua` / `ir-chapter-pagebreak.lua`（05/06）→ `postprocess_handoff_docx` → LibreOffice** 产出；本文用于 **`01`–`03`、`05`、`06` 的 CN+EN** 以及 **`07`（矢量补充）、`08`（索引壳）** 的**人工**翻页验收（**07/08** 无 Markdown 长文时，以本清单「通用检查」节为准）。

**验收阶次**：以本地导出的 `docs/fundraising/external/export-ready/*.pdf` 为准（工程规划 **① 本地**）；不将本清单勾满冒充 **② 测试网**或**③ 生产**分发已收口。

**MD → PDF 同步（P0）**：凡改动 `docs/fundraising/external/**/*.md`（含 `en/`）中与投资人叙事相关的段落，须在**同一 `release`** 下重跑 `build-investor-ir-pdf-pack.py`（及 Deck/IC 脚本若涉及），并在发 zip 前完成本节勾选；避免「仓库 Markdown 与 `export-ready` PDF」双轨口径。

**CN / EN 页数差分（P1）**：同一 `base`（如 `03-FAQ`）的 CN 与 EN PDF 页数若相差 **>1**，须在发行说明或本表备注栏写清原因（表宽、免责声明块、译文长度等），并优先检查是否产生**不可读裁切**。

**Deck / IC 印刷烟测（P2）**：`04` / `04-IC` 的 PDF 若用于线下印刷，建议至少 **一次** A4 打样核对字重与细线（脚本导出与投影场景不同）；本项**不**替代印刷厂 RIP 合同。

---

可选：发版前运行 `python scripts/tools/ir_pdf_pagecount_diff.py`（若存在）对 `export-ready/` 同号 **CN/EN** PDF 做页数对拍并留存终端输出。

## 页数目标（软目标 + 口径）

| 文档 | 目标页数 | 说明 |
|------|-----------|------|
| 06 Whitepaper | 约 **15–25** | 版式与章节首页分页会拉长；明显偏离时优先评估正文密度。 |
| 05 Litepaper | 约 **12–14** | **接受区间**；不再为压页牺牲可读性。 |
| 02 Executive Summary | 无硬编码 | 信息完整、无脏页为先。 |

**如何量页**：阅读器页码或 `pypdf` / `pdfinfo`；以 **A4、当前边距** 下页码为准。

---

## 工具链已做修补（仍须人工翻页确认）

- **目录**：`toc: false` + **`ir-native-toc.lua`** 将目录写成**可见列表与内链**（LibreOffice 不展开 Word `TOC` 域时 PDF 不再空白）；免责声明标题 `{.unnumbered .unlisted}` **不列入**目录条目；正文编号仍由 `numbersections` 起算。
- **版式顺序**：封面 → **目录** → **免责声明** → 正文（由 native-toc 与前置分页块拼装）。
- **表格**：首行标记 **`w:tblHeader`**，跨页时尽量**重复表头**（取决于 LO 渲染）。
- **标题悬底 / 孤行**：导出后对 **Heading*** 统一 `keep_with_next` / `keep_together` / `widow_control`；对 **Normal** 等开 `widow_control`。
- **封面与首页**：`reference-ir.docx` 使用「首页不同」页眉页脚；**装订顺序**：封面（仅标题）→ 目录 → 免责声明 → 正文。

---

## 每份 PDF 通用检查（逐页）

- [ ] **封面**：标题/副标题居中、品牌色清晰；封面**无**页眉页脚条。
- [ ] **法律免责声明**：全文可读；**语言版本**主免责语文与材料语言一致（中英对照块顺序符合 CN/EN 构建）。
- [ ] **目录**：「目录 / Contents」；**不含**免责声明条目；层级与正文 **H1–H3** 一致；无空项。
- [ ] **页眉**：正文段 `TravelTrust` + IR + Confidential；与封面/目录叠放观感可接受。
- [ ] **页脚**：页码连续 + `General information only — not an offer`。
- [ ] **章节首页**：05/06 在 **`##` 两位数字章**（含 `03附`）前有分页。
- [ ] **标题与紧随段落**：无明显「标题在页底、正文在下一页」的悬底。
- [ ] **正文**：混排无缺字；行距/段距均匀。
- [ ] **引用框**：`Quote` 样式一致。
- [ ] **表格**：宽表不裁切；**跨页**处有表头或至少不断裂成单字行。
- [ ] **分页**：无表格仅一行孤立在次页；列表无断头露尾。

---

## 02 Executive Summary 专项

- [ ] CN+EN：过渡与表格/列表完整。
- [ ] 口径与对外 MD 一致（代币边界、费路由叙述）。

## 05 Litepaper 专项

- [ ] 页数约 **12–14**。
- [ ] 章节分页观感到位。

## 06 Whitepaper 专项

- [ ] 页数约 **15–25**。
- [ ] **45% / 55% / 100%** 上下文清晰；宽矩阵表可跨页但可读。
- [ ] `03附` 等章节仍触发新页。

---

## 回归构建与唯一外发包（Release v1.3）

```bash
python scripts/tools/build-investor-ir-pdf-pack.py
python scripts/tools/build-investor-pitch-deck.py
python scripts/tools/build-investor-demo-video.py   # 若本机流水线可用
python scripts/tools/export-investor-dataroom.py --zip --omit-markdown
```

产物：`dist/TravelTrust-Investor-Materials-v1.3.zip`（与 registry `release` 对齐）。

可选：`pip install python-docx`（参考 DOC + handoff 后处理）。

---

## 维护说明

- 参考模板：`scripts/tools/assets/investor-ir/reference-ir.docx`
- 物化目录：`scripts/tools/assets/investor-ir/ir-native-toc.lua`（LibreOffice PDF 可检出的目录正文）
- 章节分页：`scripts/tools/assets/investor-ir/ir-chapter-pagebreak.lua`（仅 05/06）
- Handoff 后处理：`investor_ir_pandoc_pdf.postprocess_handoff_docx`

| 记录 | 日期 | 说明 |
|------|------|------|
| CN+EN QA + 外发包 | 2026-05-15 | native-toc / 表头重复 / 分页加强；zip 见 `dist/` |

人工勾选（维护者完成）：02 CN · 02 EN · 05 CN · 05 EN · 06 CN · 06 EN
